# Auto-generated display configuration
# To customize displays, run: ~/.config/hypr/scripts/setup-displays.sh
# To see available monitors use: hyprctl monitors all

# Default configuration - uses preferred resolution for all monitors
monitor = DP-3, 5120x1440@239.76, 0x0, 1

# Example configurations (uncomment and modify as needed):
# monitor = DP-1, 2560x1440@144, 0x0, 1
# monitor = HDMI-A-1, 1920x1080@60, 2560x0, 1
# monitor = eDP-1, disable  # Disable laptop screen when external monitor connected

# Workspace assignments (optional):
# workspace = 1, monitor:DP-1
# workspace = 2, monitor:HDMI-A-1
